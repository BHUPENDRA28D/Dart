import 'dart:io';

void main() {
  // List is similar like array but gives as some more functionalty to work with .
  // Declaring list

  var listNumbers = [10, 20, 30, 40];
  stdout.write(listNumbers);
}
